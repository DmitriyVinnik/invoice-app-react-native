import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  picker: {
    width: '80%',
    height: 60,
  },
  container: {
    flex: 1,
    alignItems: 'center',
    padding: 10,
  },
  errorText: {
    color: 'red',
  },
});

export default style;