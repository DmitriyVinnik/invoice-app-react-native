import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  picker: {
    width: '100%',
  },
  container: {
    width: '100%',
    height: 60,
  },
  errorText: {
    fontSize: 10,
    color: 'red',
  },
});

export default style;