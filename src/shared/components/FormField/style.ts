import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  errorText: {
    color: 'red',
  },
  textInput: {
    width: '50%',
    height: 30,
    marginBottom: 10,
    padding: 3,
    paddingLeft: 10,
    borderWidth: 2,
    borderColor: '#5d0756',
    borderRadius: 5,
  },
});

export default style;