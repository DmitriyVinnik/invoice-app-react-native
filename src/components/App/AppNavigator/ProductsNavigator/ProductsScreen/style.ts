import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  container: {
    flex: 1,
  },
  list: {
    marginTop: 10,
    marginBottom: 30,
  },
  editPanel: {
    position: 'absolute',
    bottom: 0,
    left: 10,
    right: 10,
  },
});

export default style;