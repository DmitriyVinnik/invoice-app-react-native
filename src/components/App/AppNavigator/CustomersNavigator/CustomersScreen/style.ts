import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  container: {
    flex: 1,
  },
  list: {
    marginTop: 10,
    // marginBottom: 30,
  },
  editPanel: {
    position: 'absolute',
    padding: 10,
    bottom: 0,
    left: 0,
    right: 0,
  },
});

export default style;