import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  picker: {
    width: '60%',
    height: 60,
  },
  container: {
    flex: 1,
    alignItems: 'center',
    padding: 10,
  },
});

export default style;